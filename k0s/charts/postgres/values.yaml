image:
  repository: postgres
  tag: "16"
  pullPolicy: IfNotPresent

# Service
service:
  type: LoadBalancer
  port: 5432

# Persistence
persistence:
  enabled: true
  size: 10Gi
  storageClass: longhorn

# Resources
resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

# Vault settings
vault:
  enabled: true
  role: postgres
