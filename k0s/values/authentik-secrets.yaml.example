# Authentik secrets
# Copy to authentik-secrets.yaml and fill in values
authentik:
  secret_key: "your-secret-key-here"
  postgresql:
    password: "your-db-password-here"

postgresql:
  auth:
    password: "your-db-password-here"  # Same as above
