# OS/IDE junk
.DS_Store
.idea/

# Terraform state (contains all secrets in plaintext)
**/*.tfstate
**/*.tfstate.backup
**/.terraform/

# Terraform variables (contain API keys, tokens)
**/terraform.tfvars

# Helm values secrets files
k0s/values/*-secrets.yaml

# Helm generated files (rebuilt by helmfile deps)
k0s/charts/*/Chart.lock
k0s/charts/*/charts/

# Environment files (contain secrets)
*.env

# Keep example files
!**/*.example
!**/terraform.tfvars.example
!k0s/values/*-secrets.yaml.example
